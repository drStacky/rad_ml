# For each img-lbl pair in json: img_path, lbl_path, kind
json_pth: s3://<bucket>/<key>/data.json
batch_size: 2
num_epochs: 3
# Use subset of full datasets
trn_pct: 1.0
val_pct: 1.0
tst_pct: 1.0
# Where to write training logs
log_dir: s3://<bucket>/<key>/tb_logs/